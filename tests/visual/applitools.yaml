### applitools.yaml 1.2
api_key: ${{ secrets.APPLITOOLS_API_KEY }}

propagate_eyes_test_results: true

batch:
  name: "Visual Tests Robot Framework"

app_name: ServeRest

web_ufg:
  runner_options:
    test_concurrency: 5

  browsers:
    desktop:
      - browser_type: CHROME
        width: 1920
        height: 1080